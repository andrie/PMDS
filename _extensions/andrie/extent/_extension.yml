name: extent

title: extent
author: Andrie de Vries
version: 0.1.0
quarto-required: ">=1.2.222"
contributes:
  filters:
    - extent.lua
  revealjs-plugins:
    - name: extent
      config:
        extent:
          enabled: true
          viewport: "wheat"
          background: "black"